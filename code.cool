extract Console

sum = () => {
    total
    arguments.forEach(item => total += item)
    print(total)
}

sumDef(1, 2, 3, 4, 5, 6)

function sumDef () {
    total
    arguments.forEach(item => total += item)
    print(total)
}