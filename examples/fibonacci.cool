// Extract the 'print' function into the global scope
extract Console

n = 10

// Prints the first n fibonacci numbers

print("The first " + n + " fibonacci numbers")

a = 0
b = 1

while counter < n {
    print(b)
    
    temp = b

    // The next fibonacci is found by adding the previous two
    b = a + b

    a = temp

    counter += 1
}

// Alternatively...

// a = 0 b = 1

// n = 10

// print(while 1 + n -= 1 {
//     temp = b
//     b += a
//     a = temp
// })
